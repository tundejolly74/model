Flask==2.2.3  # Always check for the latest stable version that is compatible with your app
numpy==1.25.0  # Upgrade to a compatible version with Python 3.11
scikit-learn==1.1.3  # Upgrade to a compatible version
